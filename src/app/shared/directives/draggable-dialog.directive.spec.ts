import { DraggableDialogDirective } from './draggable-dialog.directive';

describe('DraggableDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggableDialogDirective();
    expect(directive).toBeTruthy();
  });
});
